{
  "name": "fittrack",
  "version": "2.1.0",
  "private": true,
  "description": "Personal calorie, weight and habit tracker (PWA + Capacitor iOS)",
  "scripts": {
    "stage": "node scripts/stage-www.js",
    "sync": "npm run stage && npx cap sync ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  }
}
